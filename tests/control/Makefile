# tests/control/Makefile — host unit tests for lib/control (PID + plant).
# Delegates all rules to ../host.mk ( shared infra).
MODULE    := control
SRCS      := ../../lib/control/pid.c ../../lib/control/plant.c
TEST_SRCS := test_control.c
INCLUDES  := -I../../lib/control
include ../host.mk
