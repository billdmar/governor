# tests/safety/Makefile — host unit tests for lib/safety (the safety SM).
MODULE    := safety
SRCS      := ../../lib/safety/safety.c
TEST_SRCS := test_safety.c
INCLUDES  := -I../../lib/safety -I../../lib/common
include ../host.mk
