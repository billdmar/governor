# tests/config/Makefile — host tests for lib/config (persistent config, F15).
MODULE    := config
SRCS      := ../../lib/config/config.c ../../lib/proto/crc16.c
TEST_SRCS := test_config.c
INCLUDES  := -I../../lib/config -I../../lib/proto
include ../host.mk
